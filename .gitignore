# --- Binaries ---
*.exe
*.exe~
*.dll
*.so
*.dylib

# --- IDE ---
.idea/
*.iml
*.iws
*.ipr
.vscode/
*.code-workspace
*.sublime-project
*.sublime-workspace

# --- Other ---
# go.work is committed: with no replace directives left, the adapters resolve the core through the workspace.
# go.work.sum is not - every module's own go.sum already covers a workspace build.
go.work.sum
*.test
*.out
.env
vendor/
var/
bin/
benchmarks/bin/
*.log

# --- System ---
.DS_Store
Thumbs.db
*~

# --- Local overrides ---
docker/docker-compose.override.yml
